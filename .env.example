# Not used yet — this build runs entirely on in-memory mock data.
# Wire these up when the Express/MongoDB backend is ready.

VITE_API_BASE_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
